import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    marginBottom: 22,
    backgroundColor: "#F3F4F6",
  },
  outline: {
    borderRadius: 16,
    borderWidth: 0,
  },
});